server:
  port: 8888

spring:
  activemq:
    broker-url: tcp://192.168.80.71:61616 # MQ服务器地址
    user: admin
    password: admin
  jms:
    pub-sub-domain: false # false=Queue(默认) true = Topic

# 自定义队列名称
myqueue: springboot-activemq-queue